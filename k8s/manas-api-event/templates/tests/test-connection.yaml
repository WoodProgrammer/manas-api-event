apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "manas-api-event.fullname" . }}-test-connection"
  labels:
    {{- include "manas-api-event.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "manas-api-event.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
