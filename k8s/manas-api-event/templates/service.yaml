apiVersion: v1
kind: Service
metadata:
  name: {{ include "manas-api-event.fullname" . }}
  labels:
    {{- include "manas-api-event.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "manas-api-event.selectorLabels" . | nindent 4 }}
